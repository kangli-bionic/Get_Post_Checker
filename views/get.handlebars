<h1>GET Request Received</h1>

<table>
	{{#if data.length}}
		<tr>
			<th>Parameter Name</th>
			<th>Parameter Value</th>
		</tr>
	{{/if}}
	{{#each data}}
		<tr>
			<td>{{this.name}}</td>
			<td>{{this.value}}</td>
		</tr>
	{{/each}}
</table>